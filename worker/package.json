{
  "name": "sgs-test-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create sgs-tests",
    "db:migrate": "wrangler d1 execute sgs-tests --file=../schema.sql",
    "db:migrate:remote": "wrangler d1 execute sgs-tests --remote --file=../schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.68.0"
  }
}
